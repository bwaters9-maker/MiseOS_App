# Approved Feature Map Audit — IncendiumPhi (`C:\dev\miseos`)

## DAILY OPERATIONS

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Dashboard (ChefDashboard.tsx) | Active | Partial | `src/ChefDashboard.tsx` (viewMap `dashboard-home`), `AppHeader.tsx` navItems | No 86'd-items section anywhere, despite the map bullet listing "86'd items" as a Dashboard element. |
| Crib Sheet (DailyCribSheet.tsx) | Active, Build Order #2 ✓ | Partial | `src/DailyCribSheet.tsx` (viewMap `dashboard`, reached only via Dashboard Quick Action) | Same 86'd-items gap as above. Also: `items86`/`Item86` has a live `onSnapshot` listener in `useKitchenState.ts` with **zero UI consumers anywhere** — dead data being fetched every session. |
| Kitchen Timers (multi-station) | Active | Full | `src/KitchenTimers.tsx` (viewMap `timers`) | Firestore-backed, station list from `station_presets` w/ fallback. Still dark-zinc (documented as pending brand pass). |
| Alert History | Active | Full | `src/HistoricalAlerts.tsx` (viewMap `alert-history`) | Live filters + resolve/reopen, matches CLAUDE.md exactly. |

## FEATURES (Nightly Specials)

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Course/description/cost/price, active date range, 86 mid-service | Active, Build Order #3 ✓ | Full | `src/Features.tsx` | 86 toggle is a live Firestore write; both Dashboard and Crib Sheet update immediately. Distinct from the dead `items86` collection above — don't conflate the two. |

## RECIPE SYSTEM

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Recipe Builder w/ [Suggest Ingredients]/[Write Method] AI | Active | Full | `src/Recipes.tsx:639,809` | Buttons are actually labeled **"Build From Pantry"/"Draft Method"** — the feature-map bullet text itself is stale, not the code (CLAUDE.md's own prose elsewhere already uses the correct names). |
| Live Cost Analysis panel | Active | Full | `src/Recipes.tsx`, `src/lib/costEngine.ts` | Golden-split (61.8/38.2) layout confirmed. |
| FDA Nutrition Label | Active | Full | `src/components/recipes/NutritionLabel.tsx`, `src/lib/nutritionEngine.ts`, `src/lib/fdaRounding.ts` | Menu recipes only, as documented. |
| Recipe Collections (seasonal, one active) | Active, Build Order #16 pt.1 ✓ | Full | `src/RecipeCollections.tsx`, `src/hooks/useRecipeCollections.ts` | Batched write enforces exactly one `active:true`. |
| Recipe Sharing (read-only link) | Planned, deferred (#16 pt.2) | Not built | — | Genuinely absent, zero scaffolding. Deferral claim accurate. |

## INGREDIENTS — MASTER PANTRY

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| "Static, human-verified only" | Active | **Contradicted** | `AiIngredientLookup.tsx`, `IngredientForm.tsx`, `InvoicePriceUpdate.tsx` | Ingredients are routinely AI-proposed and land unverified (`regional-estimate`, `lastVerified:''`) until chef-confirmed. The map bullet is stale/aspirational — CLAUDE.md's own Master Pantry Mandate section already walks this back. |
| Purchase unit / yield% / cost-per-usable-unit | Active | Full | `src/lib/costEngine.ts` (`computeCostPerBaseUnit`) | Includes piece-weight/pack-out math. |
| Nutrition/100g, allergens, vendor links | Active | Full | `IngredientForm.tsx` (`NutritionAllergenSection`) | Plus undocumented `nutritionSource:'ai'\|'manual'` provenance field — extra, but consistent with the rest of the provenance system. |
| Invoice Price Update | Active, Build Order #7 ✓ | Full | `components/ingredients/InvoicePriceUpdate.tsx` | Matches spec closely: exact-match only, blank-cost guard, pack-mismatch warning, per-row accept. |
| **Regional Price Seed** (bundled dataset + multiplier table + onboarding, no runtime calls) | Active | **Not built** | `AiIngredientLookup.tsx` | Biggest gap in the audit. Only the `priceSource:'regional-estimate'` enum value exists. No bundled dataset, no multiplier table, no onboarding flow. What's actually stamped `regional-estimate` comes from a **live runtime `/api/ai` call** — the opposite of the map's "no runtime external calls" claim. Compare `src/lib/yieldReference.ts`, which *is* a genuine static bundled dataset — no price equivalent exists. |
| Ingredient price provenance | Active | Full | `types.ts` (`PriceSource`, `lastVerified`) | Consistently stamped by all three write paths. |
| **Vendor generic placeholders** (Broadline Distributor, etc.) | Active | **Not built** | `src/Vendors.tsx` | Zero seed data found. `Vendors.tsx` itself states inline that this is "an onboarding-time feature, not part of this build" — an acknowledged, on-the-record contradiction with the map bullet. |

## MENU

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Active recipes by category, FC% color coding | Active | Full | `src/Menu.tsx` | Driven by `isRecipeOnMenu`. |
| Guest Preview (Classic/Clean, print) | Active | Full | `components/GuestMenuPreview.tsx` | No cost/FC fields present anywhere in the component; only priced items shown. |

## STAFF & SCHEDULING

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Employee directory | Active, Build Order #4 ✓ | Full | `src/Staff.tsx` | Matches map exactly. |
| Planned shifts (any distance ahead) | Active | Full | `src/Staff.tsx` (`isShiftValid`) | No future-date cap. |
| Week/month calendar merged w/ Events | Active (part 2) | Full | `components/staff/ScheduleCalendar.tsx` | Read-only, no new collection. |
| Weekly hours + overtime flag | Active (part 2) | Full | `ScheduleCalendar.tsx` (`WeeklyHoursStrip`) | Flat 40-hr threshold, no rate shown (narrower than map technically allows, matches CLAUDE.md's stated scoping). |
| Rate never on Crib Sheet/print | Verification | **PASS** | — | Zero `hourlyRate` hits in `DailyCribSheet.tsx`/`GuestMenuPreview.tsx`. |
| Feeds Crib Sheet, no rates | Active | Full | `DailyCribSheet.tsx` | |
| Excluded: availability/swaps/notifications/time-off | Verification | **PASS** | — | Zero hits in live code; only orphaned unimported Base44 `.js` scaffold references (`src/lib/schema.js`, `DishDevelopment.js`, `OrderSheets.js`). |

## EVENTS & CLIENTS

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Merged events+catering, no standalone module | Active, Build Order #5 ✓ | Full | `src/EventCalendar.tsx` | Confirmed no separate catering code exists. |
| Customizable event types | Active | Full | `Settings.tsx`, `hooks/useEventTypes.ts` | `Settings.tsx` duplicates its own local type + seed logic rather than importing the shared hook — same duplication pattern as `station_presets`/`recipe_categories`, not a bug. |
| Client profiles + flag note | Active | Full | `types.ts` `Client`, `EventCalendar.tsx` | Amber flag line confirmed in both card and detail view. |
| Optional client link (walk-ins) | Active | Full | `EventCalendar.tsx` `EventForm` | Explicit "walk-in event" empty state. |
| Event detail view: header + 2×2 grid | Active | Full | `components/events/EventDetailView.tsx` | Grid matches Timeline/Menu/Client/Change Log exactly. |
| Event Timeline | Active | Full | `EventDetailView.tsx` `TimelinePanel` | |
| Tentative Menu | Active | Full | `EventDetailView.tsx` `MenuPanel` | Cost projection footer labeled "linked items only" as documented. |
| Client panel | Active | Full | `EventDetailView.tsx` `ClientPanel` | |
| Change Log (append-only) | Active | Full | `EventDetailView.tsx` `ChangeLogPanel` | Genuinely no edit/delete UI on existing entries. |
| Quote generation | Planned, deferred | Not built | — | Genuinely absent. |
| Feeds Crib Sheet | Active | Full | `DailyCribSheet.tsx` | Milestones rendered compactly beneath each event row. |

## VENDOR MANAGEMENT

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Supplier directory | Active, Build Order #11 ✓ | Full | `src/Vendors.tsx` | `orderDays` is a structured chip multi-select, matches map's "no open text" principle. |
| Vendor detail (linked ingredients) | Active | Full | `Vendors.tsx` | Inline expand, no separate page. |
| Master Pantry link, 2-step unlink delete | Active | Full | `Vendors.tsx`, `IngredientForm.tsx` | `deleteField()` clears `vendorId` before vendor doc delete — no orphaned references. |

## SETTINGS — RESTAURANT PROFILE

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Identity (name/chef/color/logo) | Active | Full | `Settings.tsx:346-460` | Logo row correctly shown as disabled "Pending" line, not a broken control. |
| Kitchen Context (cuisine/price point/FC%) | Active | Full, IA mismatch | `Settings.tsx:376-408, 522-539` | Fields all wired, but there's no literal "Kitchen Context" heading — cuisine/price point live under "Restaurant Profile," targetFcPercent lives under a separate "Recipe Costing" card. Cosmetic only. |
| Regional Intelligence + AI injection | Active | Full | `src/lib/regionContext.ts` | All 6 claimed injection sites confirmed present; correctly excluded from the raw invoice OCR prompt. |

## AI LAYER

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Test Kitchen / Dish Optimizer | Active | Partial | `TestKitchenHub.tsx:296-830` | Playground chat is real. Plate Design / Ingredient Palette / "Trends for This Dish" / Recipe Build panels are static placeholders — an acknowledged, documented gap, not a surprise. |
| Culinary Trends & Forecasts (Phase B) | Active ✓ | Full | `TestKitchenHub.tsx:22-434` | Draft+verify two-pass, exactly-one Viral Bridge, citation-only sources, weekly auto-refresh, 12-doc history cap — all confirmed. |
| Sous persona (Playground chat) | Build Order #12 — **not marked ✓**, unlike surrounding items | Full, cosmetic drift | `src/lib/sousPersona.ts`, `TestKitchenHub.tsx:740` | Persona shipped and wired; no standalone Chef Chat surface exists (confirmed absent, correctly). Chat header displays "Chef Matthew — Sous Chef" rather than "Sous" — a named placeholder pending a future onboarding customization setting, per an inline comment. |
| Ingredient Advisor | Active, Build Order #13 ✓ | Full | `components/ingredients/IngredientAdvisor.tsx` | Web-search-grounded, zero writes, disclaimer present. |
| `/api/ai` proxy boundary (no Firestore, no `VITE_` key, no direct `src/` fetch) | Architectural constraint | **PASS** | `server.ts` | Confirmed clean on all three points. |

## PERMANENTLY PURGED — NEVER REBUILD

| Feature | Map Status | Repo Status | Files | Notes |
|---|---|---|---|---|
| Handover Log | Purged | Absent from live code, **stub leftover** | `src/components/handover/HandoverLog.jsx` | Empty (0-byte), unimported — dead folder/filename artifact of the purged feature. Harmless but worth a cleanup pass. |
| Live POS Sync | Purged | Absent | — | Clean, zero hits, no SDK in `package.json`. |
| Automatic Reservation Sync | Purged | Absent | — | Clean. |
| Financial Smoke Detector | Purged | Absent | — | Clean. |
| Market Volatility Tracking | Purged | Absent, **stub leftover** | `src/lib/schema.js` | Unimported file contains leftover `price_trend`/`culinary_trend`/`price_alert.is_volatile` shape reminiscent of this purged feature — dead code, not wired to anything live. |
| Training Dashboard | Purged | Absent | — | Clean. |
| Hostess Chat | Purged | Absent | — | Clean. |

## Code found that is NOT on the Feature Map (dead/orphaned, this pass)

| Finding | Files | Notes |
|---|---|---|
| Empty stub files, unimported | `src/components/StationMatrix.js`, `src/components/InsightRail.js` | 0-byte, harmless dead weight. |
| Legacy scaffold components, unimported | `src/components/Header.js`, `OrderSheets.js`, `DishDevelopment.js` | Non-empty Base44/Gemini-era widgets, missed by the prior cruft audits because those only swept `.jsx` folders, not stray `components/*.js` files. |
| Duplicate unwired Test Kitchen screen | `src/components/testkitchen/DishOptimizer.jsx` | Full alternate implementation, never imported — `TestKitchenHub.tsx` is the real reachable view. |
| Unused data-contract stub | `src/lib/schema.js` | Also the Market Volatility leftover noted above. |
| Unused white-label config stub | `src/lib/app-params.js` | Hardcodes the stale "MiseOS"/"MiseOS Operations" brand name and reads a `miseos_brand_config` localStorage key — never imported, so it isn't actually the `APP_PARAMS` mechanism the brand kit rules assume exists. |
| Component cited in CLAUDE.md as a live pending-brand-pass screen, but has zero importers | `src/components/StationPassHeader.tsx` | CLAUDE.md's Brand v1.1 migration-status note references this file's "IncendiumPhi" text as if it renders somewhere reachable. It doesn't appear to be wired into any nav path — worth confirming whether it's genuinely dead or just missing from `App.tsx`. |

---

**Three things stand out as real, non-cosmetic gaps:** the Regional Price Seed feature (map claims a static bundled dataset; none exists — what's there is a live AI call, functionally the opposite), the seeded-vendor-placeholders bullet (explicitly acknowledged as unbuilt in the code's own comments), and the missing 86'd-items section on both Dashboard and Crib Sheet despite the map listing it by name (compounded by the fact that `items86` has a live listener with no UI consumer anywhere — that data is being fetched for nothing).
